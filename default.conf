server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;

    sendfile off;
    absolute_redirect off;

    location = / {
        return 302 /Log/log.html;
    }

    location / {
        try_files $uri $uri/ =404;
    }
}